package main

import (
	"fmt"
	"strings"
)

func main() {
	input := "Claire"
	upper := strings.ToUpper(input)
	lower := strings.ToLower(input)
	fmt.Println("Before: " + input)
	fmt.Println("After: " + upper)
	fmt.Println("Before: " + lower)
}
